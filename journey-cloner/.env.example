# Paste a fresh token from your current browser session.
# Do not commit/share this file.
AUTH_TOKEN=PASTE_FRESH_BEARER_TOKEN_HERE

BASE_URL=https://pmi.rea-backoffice.gr8.tech/api/ubo/api/v0/crm/journey-builder/v0
BRAND=JBCL
TIMEZONE=America/Santiago

# Optional. Only use if Bearer token alone fails from VPS.
COOKIE=

# prediction_campaign.py -- identifies the EXISTING MultiNumberPrediction
# draft/content/front you're editing. Not secret (no auth token needed: the
# generated console script captures the Bearer token from the browser tab
# itself), just config -- can also be passed as --draft-id/--content-id/--front-id.
DRAFT_ID=
CONTENT_ID=
FRONT_ID=
